---
const defaultLocale = 'en';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Redirecting...</title>
    <meta name="robots" content="noindex" />
    <link rel="canonical" href="https://novameat.com/en" />
    <script>
      function redirectToDefaultLocale() {
        console.log("🚀 Redirigiendo a /en");
        window.location.replace("/en"); // ✅ reemplaza en lugar de añadir entrada al historial
      }

      // Redirige normalmente en primer acceso
      setTimeout(redirectToDefaultLocale, 500);

      // Redirige también si el usuario vuelve atrás y la página se restaura desde memoria
      window.addEventListener("pageshow", (event) => {
        if (event.persisted) {
          console.log("♻️ Volviendo desde el historial (bfcache)");
          setTimeout(redirectToDefaultLocale, 100);
        }
      });
    </script>
  </head>
  <body>
    
  </body>
</html>
